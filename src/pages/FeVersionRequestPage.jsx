import React, { useState } from "react";
import BasicForm from "../components/BasicForm";

const FeVersionRequestPage = ({ title }) => {
  return <BasicForm title={title} />;
};

export default FeVersionRequestPage;
